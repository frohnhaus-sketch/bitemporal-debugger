export type InvestigationPresentation = {
  title: string;
  subtitle: string;
  consequences: string[];
  rootCause: string;
  businessImpact: string;
  recommendation: string;
  nextSteps: string[];
};